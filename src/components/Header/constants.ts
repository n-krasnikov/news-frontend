import { ModalType } from '../../vite-env';

export const SIGN_IN: ModalType = 'Sign In';
export const SIGN_UP: ModalType = 'Sign Up';

export const BUTTONS = [SIGN_IN, SIGN_UP];
export const TOKEN = 'token';
